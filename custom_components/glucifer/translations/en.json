{
  "title": "Glucifer HA",
  "config": {
    "step": {
      "user": {
        "title": "Connect a JugglucoNG phone",
        "description": "Home Assistant will receive glucose data from JugglucoNG on this phone. Give the phone a name, such as My phone. Add each phone separately.",
        "data": {
          "name": "Phone name"
        }
      },
      "save_connection": {
        "title": "Activate the phone connection",
        "description": "Choose your settings and select Submit to activate the connection. The next screen shows its QR code and connection URL for pairing with JugglucoNG. You can reopen them later through Configure for this phone.\n\nTo connect away from home, use a reachable HTTPS address or VPN and leave Accept local requests only turned off.",
        "data": {
          "local_only": "Accept local requests only",
          "stale_seconds": "Mark data unavailable after (seconds)",
          "glucose_unit": "Glucose display unit"
        }
      }
    },
    "error": {
      "name_required": "Enter a name."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Phone connection settings",
        "description": "This connection is ready to receive data. In JugglucoNG, open API destinations, add Glucifer HA, and scan the QR code below or paste this connection URL into POST URL:\n\n`{url}`\n\nTurn the destination on and choose which data to send. JugglucoNG saves changes automatically. Tap Send test message and check Last success, then tap Done.\n\nYou can close this HA dialog after scanning. Submit only if you changed the settings below. If only a URL path is shown, add your Home Assistant URL before it. Keep the URL and QR code private.",
        "data": {
          "local_only": "Accept local requests only",
          "stale_seconds": "Mark data unavailable after (seconds)",
          "glucose_unit": "Glucose display unit",
          "rotate_secret": "Replace the connection URL",
          "qr_code": "Connection QR code"
        }
      },
      "rotate": {
        "title": "Replace the connection URL",
        "description": "Submit to activate this new connection URL and disable the old one. Then update the Glucifer HA destination in JugglucoNG using the QR code below or this URL. Your entities and stored readings are kept.\n\n`{url}`\n\nKeep the URL and QR code private.",
        "data": {
          "qr_code": "Connection QR code"
        }
      }
    }
  }
}
